[build]
builder = "nixpacks"
buildCommand = "npm install --force && rm -rf node_modules/.cache && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
